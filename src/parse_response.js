function parseResponse(res) {
  // const transit = ProtoBuf.protoFromFile('gtfs-realtime.proto').build('transit_realtime');
  // const msg = transit.FeedMessage.decode(data);
  console.log(res);
}

export default parseResponse;
